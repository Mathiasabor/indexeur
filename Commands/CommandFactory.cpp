//
// Created by abor on 05/01/25.
//

#include "CommandFactory.h"
#include "QueryingCommands/SearchCommand.h"
#include "Options/Options.h"

void CommandFactory::createCommand() {
/*
    if (tokens.first() == "SEARCH") {
       SearchCommand command = SearchCommand();
       QString argument = tokens.at(1);
       command.searchQuery.append(argument);

       QList<QString> commonOptions = findCommonOptions(tokens);
       if(commonOptions.size() > 0){
           for (const auto& option : commonOptions) {
               command.searchQuery.append(" " + option);
           }
       }




    }

    */
}